# MaxGameStudio 开发记录

## 项目边界

- 参考项目与许可证记录统一放在根目录 README 的“参考项目与致谢”小节；本文不把参考项目描述为主体代码或桌面架构来源。
- 参考 `akiver/cs-demo-manager` 的成熟工作流，但不复制其 PostgreSQL 数据层；当前项目继续使用已有 SQLite 数据库。
- 功能分阶段落地：官匹 Demo 获取与自动入库 → 分析/回放整合 → 灵敏度测试与个性化建议 → 磁轴键盘测试与关联分析。
- GitHub 仓库在开发完成前保持 Private；转为 Public 必须由项目所有者再次明确确认。

## 2026-08-02：阶段 1 — Share Code 下载垂直切片

已完成：

- 建立私人仓库 `INEEDBUG/MaxGameStudio`，项目远程与发布路径统一使用当前品牌。
- 将用户提供的 `steam://rungame/...+csgo_download_match CSGO-...` 输入规范化并解码为 match ID、reservation ID 和 TV port。
- 通过独立、按需下载的 `@akiver/boiler-writter` 与本机 Steam Game Coordinator 通信，读取 Valve 返回的真实 `.dem.bz2` 地址。
- 对 npm 包做固定版本和 SHA-512 完整性校验，只提取白名单文件。
- 下载后原子解压为 `.dem`，沿用原项目的 Demo 监听目录与自动入库队列。
- 在官匹战绩页加入分享代码/Steam 链接输入、首次使用许可确认、成功与错误状态。
- 后端全套 683 项测试通过，前端全套 584 项测试及生产构建通过。

尚未完成：

- 在真实 Steam 登录环境中做端到端试运行；该步骤会实际下载并运行可选组件，需要用户在界面确认并关闭 CS2。
- 完善已过期比赛的诊断提示。

## 技术决定

1. 数据库继续使用项目现有 SQLite，不引入 PostgreSQL。
2. GPL-3.0 helper 不链接进 Python/Tauri 主程序，保持独立进程与独立许可文件；发布前仍需做一次完整许可证复核。
3. 不再使用由 match ID 猜测 replay 服务器的 URL 作为 Share Code 下载依据，改用 Steam Game Coordinator 返回值。

## 2026-08-02：阶段 2 — CS2 灵敏度实验室

已完成：

- 新增本地 SQLite `sensitivity_sessions` 表，保存测试输入、推荐结果、分辨率背景和置信度。
- 新增 Touch Grid 无点击甩枪测试：准星碰到目标即触发，统计反应时间、路径效率与过冲。
- 新增 Orbit Track 连续追踪测试：目标持续移动，统计有效追踪时间、平均距离和脱靶次数。
- 每轮使用 `performance.now()` + `requestAnimationFrame` 驱动独立倒计时；Pointer Lock 成功后才计时，Esc/失焦自动暂停并可恢复。
- 比较当前灵敏度的 0.8×、1.0×、1.2×，使用 CS2 `m_yaw 0.022` 生成游戏可直接使用的 sensitivity、eDPI、cm/360 和控制台命令。
- 游戏分辨率（默认用户实际使用的 1024×1080）、显示比例和拉伸方式进入测试上下文；不使用无依据的固定“分辨率灵敏度修正”。
- 中英文页面、本地历史记录和一键复制控制台命令已接入主导航。

验证：

- 后端全套 687 项测试通过。
- 前端全套 587 项测试通过，生产构建通过。
- 在本地浏览器完成 1024×1080 默认设置、靶场进入、鼠标锁定失败保护、退出恢复与视觉布局检查。

## 2026-08-02：阶段 3 — 磁轴输入实验室

已完成：

- 新增 A/D 急停交替、单键快速连点和 WASD 实战输入三种 15 秒测试模式。
- 用高精度浏览器时钟记录 W/A/S/D、Shift、Ctrl、Space 的非重复 keydown/keyup 边沿，并实时显示按键状态。
- 分析每秒按下次数、平均保持时间、保持抖动、A/D 方向切换间隔、重叠比例和异常重复/缺失边沿。
- 将键盘名称、触发行程、Rapid Trigger 按下/释放行程和实测结果写入同一个本地 SQLite 数据库，支持历史配置对比。
- 明确标注通用 Windows 键盘事件无法提供真实模拟轴毫米行程，不伪造磁轴曲线；当前版本用用户填写的参数关联数字事件表现。

验证：

- 后端全套 690 项测试通过。
- 前端全套 587 项测试通过，生产构建通过。

## 2026-08-02：阶段 4 — Demo 下载任务管理

已完成：

- 将分享代码下载改为后台任务：解析 Steam 比赛信息、流式下载、解压入库均有明确阶段和百分比进度。
- 下载流按 `Content-Length` 计算真实字节进度；服务器未提供长度时使用保守的未知总量进度。
- 新增任务查询、取消和失败/取消后重试接口；取消时清理未完成的压缩文件，避免长期占用磁盘。
- 官匹战绩页新增进度条、取消按钮、重试按钮及中英文阶段说明；临时轮询错误会自动恢复。

验证：

- 后端全套 695 项测试通过（其中下载任务与 Share Code 针对性测试 18 项）。
- 前端全套 587 项测试通过，生产构建通过。

下一步：

- 在用户关闭 CS2 并于界面确认 GPL helper 后，用真实 Steam 登录态完成一次端到端下载。
- 补充过期比赛、Steam 未登录和 Game Coordinator 忙碌时的分层诊断。
- 进入统一品牌、首次启动引导和 Windows 桌面打包阶段。

## 2026-08-02：阶段 5 — 品牌与首次启动引导

已完成：

- 用户可见名称统一为 `MaxGameStudio`，桌面窗口标题和侧栏品牌同步更新。
- 新增首次启动引导，直接提供 Demo 库、官匹下载、灵敏度实验室和磁轴输入实验室四个入口。
- 引导完成状态仅写入本机 `localStorage`，不联网、不收集账户或测试数据。
- 保留旧 Tauri identifier 与内部数据迁移兼容层，避免品牌升级导致已有 SQLite、配置和备份丢失。
- README 仅保留一个参考项目链接，并继续保留必要的许可证与作者归属信息。

验证：

- 前端全套 589 项测试通过，生产构建通过。

## 2026-08-02：阶段 6 — 私人 Windows 发布候选

已完成：

- 本机未安装 Rust/MSVC；为避免占用 C 盘，将正式构建迁移到 GitHub 托管的 Windows runner。
- `Release Windows` 支持手动输入候选版本，手动运行只上传私人 artifact，不创建公开 Release。
- 安装包品牌更新为 `MaxGameStudio`，并停用旧版 R2 自动更新通道。
- 当前候选为 `0.1.0-rc.2`：安装包 39,188,332 字节，内嵌 resources 87.19 MiB；rc.2 包含官匹过期/繁忙/Steam 状态分层诊断。
- 本机 D 盘复核 SHA-256：`990dd4cc590576cc2f6a01af9a802d85052e7ee3b0a84a436d3d1df34bde8dc2`，与云端 `SHA256SUMS` 一致。

验证：

- 后端 706 项、前端 590 项、Rust fmt/clippy/tests、内嵌 Python 导入、NSIS 生成和体积预算全部通过。
- 候选包未配置 Authenticode，当前仅用于私人验收；正式公开前必须补签名与独立更新通道。
- 详见 [`private-release-candidate.zh-CN.md`](./private-release-candidate.zh-CN.md)。
